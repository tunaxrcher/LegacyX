export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>🏥 LegacyX API Server</h1>
      <p>Core API. See <code>/api/health</code> and <code>/api/v1/*</code></p>
    </main>
  );
}
